<!DOCTYPE html>
<html>
<head>
   <title>INTRO</title>
   <style>
      header {
         color: blue;
         font-style:oblique;
         font-weight:bolder;
         text-align: center;
         height: 100px;
         line-height: 100px; /* Center the text vertically */
         color: #200350; /* Text color for the header */
         font-size: 50PX;
      }

      body {
         background-image: url('U0rczZ.jpg'); /* Replace 'your-image.jpg' with the path to your image */
         background-size: cover;
         background-position: center;
         margin: 0;
         padding: 0;
         display: flex;
         flex-direction: column; /* Stack elements vertically */
         justify-content: center;
         align-items: center;
         height: 100vh;
         background-color: #f0f0f0;
      }

      .log {
         text-align: center;
      }

      .doc, .PHARMA, .ADMIN {
         background-color: rgb(97, 10, 154);
         color: beige;
         border: none; /* Remove the border to make it look cleaner */
         border-radius: 20px;
         height: 50px;
         width: 150px;
         cursor: pointer;
         margin: 10px; /* Add margin to separate the buttons */
      }
      .doc:hover, .PHARMA:hover, .ADMIN:hover {
         background-color: rgb(245, 5, 5);
         color: beige;
         border: none; /* Remove the border to make it look cleaner */
         border-radius: 20px;
         height: 50px;
         width: 150px;
         cursor: pointer;
         margin: 10px; /* Add margin to separate the buttons */
      }
   </style>
</head>
<body>
   <header>
      PHARMA THING
   </header>
   <div class="log">
      <button class="doc">
         DOCTOR
      </button>

      <button class="PHARMA">
         PHARMASIST
      </button>

      <button class="ADMIN">
         ADMINISTRATOR
      </button>
   </div>
</body>
</html>
